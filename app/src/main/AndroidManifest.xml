<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lab03.visitadoresmedicos">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VisitadoresMedicos">
        <activity
            android:name=".views.VisitFormActivity"
            android:exported="true"
            android:label="@string/activity_visit_title"/>
        <activity
            android:name=".views.PatientFormActivity"
            android:label="@string/activity_patient_title"
            android:exported="true" />
        <activity
            android:label="@string/activity_menu_title"
            android:name=".views.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>